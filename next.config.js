/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["i.ibb.co","hfconsultancy.atlassian.net"],
    },
};

module.exports = nextConfig;
